dockers:
  - mysql
  - adminer

services:
  - node
  - php

service:
  node:
    profile: pt
    env:
      - ADMINER_PORT=9999
      - PASSWORD=1223
    run:
      - echo "Running a nodejs service"
      - cd $__ENV_ROOT__/test/
      - node ./service.js
